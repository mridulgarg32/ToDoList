import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {fontSize: 14, fontWeight: 'bold', color: 'grey'},
  wrapperContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginVertical: 10,
  },
  buttonContainer: {borderRadius: 5, borderWidth: 1},
});
export default styles;
